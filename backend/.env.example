# Database
DATABASE_URL="postgresql://chainboard:chainboard_dev@localhost:5432/chainboard"

# Server
PORT=3000
NODE_ENV=development

# Note: When running with Docker, the backend runs on port 3000 inside the container
# but is exposed as port 3001 on the host to avoid conflicts

# Slack
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_SIGNING_SECRET=your-signing-secret

# CORS
CORS_ORIGIN=http://localhost:5173
