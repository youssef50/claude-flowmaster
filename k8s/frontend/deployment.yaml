apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: kirro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: youssef50/kirro-frontend:latest
        ports:
        - containerPort: 80
